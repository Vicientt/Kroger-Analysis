version: 2

# raw.kroger_products_all = union of raw.kroger_products_YYYY_MM_DD (flat csv)
sources:
  - name: kroger_raw
    schema: raw
    description: Raw Kroger product data, csv loaded per day
    tables:
      - name: kroger_products_all
        description: "View union of raw.kroger_products_YYYY_MM_DD tables"
